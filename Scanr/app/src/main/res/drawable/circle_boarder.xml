<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:innerRadius="1dp"
    android:shape="oval"
    android:useLevel="true">
    <stroke
        android:width="1dp"
        android:color="@color/secondary_blue" />

    <solid android:color="@color/transparent" />
    <size
        android:width="40dp"
        android:height="40dp" />
    <corners android:radius="@dimen/card_album_radius" />
</shape>